server:
  port: 8080

spring:
  cloud:
    gateway:
      routes:
        - id: user
          uri: http://localhost:8081
          predicates:
            - Path=/api/users/**
        - id: product
          uri: http://localhost:8082
          predicates:
            - Path=/api/products/**
